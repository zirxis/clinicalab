# نظام تسجيل الدخول والمصادقة - Clinicalab Login System

## نظرة عامة
تم تطوير نظام تسجيل دخول وإدارة حسابات متكامل لمنصة Clinicalab يوفر جميع الميزات الأساسية للتحكم في حسابات المستخدمين.

## الميزات الرئيسية

### 1. **التسجيل والمصادقة** 📝
- تسجيل مستخدمين جدد مع التحقق من البيانات
- تسجيل الدخول بآمان
- تسجيل الخروج
- إدارة جلسات المستخدمين عبر localStorage
- فحص صحة البيانات المدخلة

### 2. **إدارة الملف الشخصي** 👤
- عرض وتحرير معلومات الملف الشخصي
- تحديث البيانات الشخصية (الاسم، الهاتف، العمر، الجنس)
- صورة ملف شخصي ديناميكية (Avatar)
- حساب عدد أيام العضوية

### 3. **إدارة الحجوزات** 📅
- عرض جميع حجوزات المستخدم
- تتبع حالة الحجز (مؤكد، قيد الانتظار، ملغى)
- تحميل الفواتير
- إلغاء الحجوزات

### 4. **المفضلة** ❤️
- إضافة مختبرات إلى المفضلة
- إزالة من المفضلة
- عرض المختبرات المفضلة

### 5. **الإعدادات والأمان** ⚙️
- تغيير كلمة المرور
- تفضيلات الإشعارات
- حذف الحساب بشكل دائم

## البنية الفنية

### الملفات الرئيسية

#### `auth.js` - نظام المصادقة الأساسي
```javascript
class AuthSystem {
  - constructor()          // تهيئة النظام
  - loadUser()            // تحميل المستخدم الحالي
  - loadUsers()           // تحميل جميع المستخدمين
  - register()            // تسجيل مستخدم جديد
  - login()              // تسجيل الدخول
  - logout()             // تسجيل الخروج
  - updateProfile()      // تحديث الملف الشخصي
  - addBooking()         // إضافة حجز جديد
  - addFavorite()        // إضافة مختبر للمفضلة
  - removeFavorite()     // إزالة من المفضلة
  - isLoggedIn()         // التحقق من تسجيل الدخول
  - getCurrentUser()     // الحصول على المستخدم الحالي
}
```

### تخزين البيانات
- **localStorage** يُستخدم لتخزين:
  - `currentUser`: بيانات المستخدم الحالي المسجل
  - `clinicalabUsers`: قائمة جميع المستخدمين المسجلين

### هيكل بيانات المستخدم
```javascript
{
  id: string,                          // معرف فريد
  email: string,                       // البريد الإلكتروني
  password: string,                    // كلمة المرور (مشفرة)
  fullName: string,                    // الاسم الكامل
  phone: string,                       // رقم الهاتف
  age: number | null,                  // العمر
  gender: string | null,               // الجنس
  createdAt: string,                   // تاريخ الإنشاء
  bookings: Array,                     // قائمة الحجوزات
  favorites: Array,                    // قائمة المفضلة
  profileComplete: boolean,            // اكتمال الملف الشخصي
  city: string | null,                 // المدينة
  bio: string | null                   // البيان الحيوي
}
```

## الصفحات الرئيسية

### 1. `index.html` - الصفحة الرئيسية
- نموذج تسجيل الدخول والتسجيل (Modals)
- عرض معلومات المستخدم في الـ Navigation
- قائمة منسدلة للمستخدمين المسجلين

### 2. `profile.html` - الملف الشخصي
- عرض معلومات المستخدم
- تحرير البيانات الشخصية
- عرض الحجوزات والمفضلة
- إعدادات الحساب

### 3. `bookings.html` - الحجوزات
- قائمة جميع الحجوزات
- تصفية حسب الحالة
- عرض تفاصيل الحجز
- تحميل الفاتورة

### 4. `search.html` - البحث عن المختبرات
- نموذج تسجيل الدخول والتسجيل
- عرض نتائج البحث
- إضافة إلى المفضلة

## المودالات (Modals)

### نموذج تسجيل الدخول
```html
<div id="login-modal">
  - حقل البريد الإلكتروني
  - حقل كلمة المرور
  - خيار "تذكرني"
  - رابط "نسيت كلمة المرور"
  - زر تسجيل الدخول
  - رابط لنموذج التسجيل
</div>
```

### نموذج التسجيل
```html
<div id="register-modal">
  - حقل الاسم الكامل
  - حقل البريد الإلكتروني
  - حقل رقم الهاتف
  - حقل العمر
  - حقل الجنس
  - حقل كلمة المرور
  - حقل تأكيد كلمة المرور
  - موافقة على الشروط والأحكام
  - زر إنشاء الحساب
</div>
```

## الدوال الرئيسية

### دوال المودالات
```javascript
openLoginModal()          // فتح نموذج تسجيل الدخول
closeLoginModal()         // إغلاق النموذج
openRegisterModal()       // فتح نموذج التسجيل
closeRegisterModal()      // إغلاق النموذج
switchToRegister()        // الانتقال من تسجيل الدخول للتسجيل
switchToLogin()          // الانتقال من التسجيل لتسجيل الدخول
```

### دوال التحقق
```javascript
validateEmail(email)      // التحقق من صيغة البريد
validatePhone(phone)      // التحقق من صيغة الهاتف
validateForm(data)        // التحقق من جميع حقول النموذج
```

## التحقق من البيانات

### عند التسجيل:
✅ الاسم: حد أدنى 2 أحرف
✅ البريد الإلكتروني: صيغة بريد صحيحة
✅ الهاتف: 10 أرقام فقط
✅ كلمة المرور: حد أدنى 6 أحرف
✅ كلمات المرور متطابقة
✅ عدم تكرار البريد الإلكتروني

### عند تسجيل الدخول:
✅ البريد الإلكتروني موجود
✅ كلمة المرور صحيحة

## المميزات الإضافية

### التنبيهات (Notifications)
```javascript
showNotification(message, type)
// أنواع: 'info', 'success', 'error'
```

### إدارة الجلسة
- الحفظ التلقائي للمستخدم في localStorage
- تحديث الـ UI تلقائياً عند تسجيل الدخول/الخروج
- التحقق من تسجيل الدخول عند دخول الصفحات المحمية

### الأمان
- كلمات المرور مشفرة بشكل بسيط (في بيئة الإنتاج، استخدم bcrypt)
- بيانات المستخدم الحساسة لا تُعرض
- التحقق من الصلاحيات قبل الوصول للصفحات

## الاستخدام

### لتسجيل مستخدم جديد:
```javascript
const result = authSystem.register({
  email: 'user@email.com',
  password: 'password123',
  confirmPassword: 'password123',
  fullName: 'أحمد محمد',
  phone: '0661234567',
  age: 25,
  gender: 'male'
});

if (result.success) {
  console.log('تم إنشاء الحساب بنجاح');
} else {
  console.log(result.errors); // عرض الأخطاء
}
```

### لتسجيل الدخول:
```javascript
const result = authSystem.login('user@email.com', 'password123');

if (result.success) {
  console.log('تم تسجيل الدخول بنجاح');
} else {
  console.log(result.errors);
}
```

### للتحقق من تسجيل الدخول:
```javascript
if (authSystem.isLoggedIn()) {
  const user = authSystem.getCurrentUser();
  console.log(user.fullName);
}
```

### إضافة حجز:
```javascript
const result = authSystem.addBooking({
  labName: 'مختبر الصحة',
  testType: 'تحاليل دم',
  price: 2500,
  date: '2024-12-15',
  time: '10:30'
});
```

### إدارة المفضلة:
```javascript
authSystem.addFavorite('lab-id-123');
authSystem.removeFavorite('lab-id-123');
```

## التكامل مع الصفحات الأخرى

### في `search.html`:
- يمكن إضافة مختبرات للمفضلة
- يمكن الحجز مباشرة بعد تسجيل الدخول

### في `booking.html`:
- يتم حفظ الحجز تلقائياً للمستخدم المسجل
- يمكن عرض جميع الحجوزات السابقة

### في `index.html`:
- يعرض اسم المستخدم في الـ Navigation
- يعرض قائمة منسدلة للمستخدم المسجل

## ملاحظات أمان مهمة ⚠️

### الحالية (Development):
- كلمات المرور تُخزن محلياً (غير آمنة للإنتاج)
- لا يوجد تشفير قوي

### للإنتاج يجب:
- استخدام قاعدة بيانات آمنة (MongoDB, PostgreSQL, إلخ)
- استخدام bcrypt أو أقوى لتشفير كلمات المرور
- استخدام JWT أو OAuth للتحقق من الجلسات
- استخدام HTTPS
- تطبيق معدل الحد من المحاولات (Rate Limiting)
- تفعيل Two-Factor Authentication (2FA)

## الخطوات القادمة المقترحة 🚀

1. ربط قاعدة بيانات حقيقية
2. إضافة إعادة تعيين كلمة المرور
3. إضافة التحقق من البريد الإلكتروني
4. إضافة تسجيل الدخول عبر وسائل التواصل
5. إضافة نظام الإشعارات
6. إضافة نظام التقييمات والمراجعات
7. تحسين الأمان والتشفير

## المساعدة والدعم 💬

للمزيد من المعلومات أو الإبلاغ عن مشاكل، يرجى التواصل مع فريق التطوير.

---

**آخر تحديث:** ديسمبر 2024
**الإصدار:** 1.0
